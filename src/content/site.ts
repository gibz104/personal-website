/**
 * Where the site lives. One constant, because a canonical URL that disagrees
 * with the sitemap or the structured data is worse than having neither.
 */
export const SITE_URL = "https://rossgibson.dev";

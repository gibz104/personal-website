import { TypeLab } from "@/components/type-lab";

export const metadata = { title: "Monogram faces" };

/**
 * Temporary comparison surface, not part of the site. Delete this route once a
 * face is chosen.
 */
export default function TypePage() {
  return <TypeLab />;
}
